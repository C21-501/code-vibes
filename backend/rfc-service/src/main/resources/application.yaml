spring:
  application:
    name: rfc-service

  datasource:
    url: jdbc:postgresql://localhost:5050/cab_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/cab-realm

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 6MB

keycloak:
  auth-server-url: http://localhost:8081
  realm: cab-realm
  resource: cab-backend
  credentials:
    secret: backend-secret-123

app:
  security:
    enabled: true
  file:
    storage-path: /tmp/uploads
    max-file-size: 5MB
    max-request-size: 6MB

logging:
  level:
    root: INFO

# Planka Integration Configuration
planka:
  enabled: ${PLANKA_ENABLED:false}
  url: ${PLANKA_URL:http://localhost:3000}
  api-token: ${PLANKA_API_TOKEN:}
  webhook-secret: ${PLANKA_WEBHOOK_SECRET:}
  project-id: ${PLANKA_PROJECT_ID:}
  board-id: ${PLANKA_BOARD_ID:}
  auto-sync: ${PLANKA_AUTO_SYNC:true}
  list-name-new: ${PLANKA_LIST_NEW:Новые}
  list-name-under-review: ${PLANKA_LIST_UNDER_REVIEW:На рассмотрении}
  list-name-approved: ${PLANKA_LIST_APPROVED:Утверждено}
  list-name-implemented: ${PLANKA_LIST_IMPLEMENTED:Выполнено}
  list-name-rejected: ${PLANKA_LIST_REJECTED:Отклонено}